$(".comment-form").remove();
$("#invoice-comments").append("<%= escape_javascript(render(partial: 'comments/comment_form', locals: {object: @comment.commentable, comment: @comment_new})) %>");
update_organizations_labels();
$("#comments").append("<%= escape_javascript(render(:partial => 'comments/comment', :locals => {:comment => @comment})) %>");
$("#comment-<%= @comment.id %>").hide();
$("#comment-<%= @comment.id %>").fadeIn();
$("#new_comment")[0].reset();