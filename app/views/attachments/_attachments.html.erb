<%= render partial: "attachments/attachment_form", locals: {object: object, attachments: @attachments} %>

<div id="attachments">
	<table class="table-attachments" style="<%= "display: none;" unless attachments.any?%>">
		<thead>
			<tr>
				<th>Nombre</th>
				<th>Fecha</th>
				<th>Autor</th>
				<th></th>
			</tr>
		</thead>
		<tbody>
			<% attachments.each do |attachment| %>
				<%= render partial: "attachments/attachment", locals: {attachment: attachment}  %>
			<% end %>
		</tbody>
	</table>
</div>
